This is a way for doing it manually.

1. Install yum cron
yum install yum-cron

2. Enable and start the yum-cron.
# sudo systemctl enable yum-cron
# sudo systemctl start yum-cron

3. Verify the service is running with the below command. 
# systemctl status yum-cron
4. check to see if yum-cron is working
# sudo tail -10 /var/log/yum.log

#####This should be present by default####################################
#cat /etc/cron.daily/0yum-daily.cron
[root@ip-172-31-8-140 yum]# cat /etc/cron.daily/0yum-daily.cron
#!/bin/bash

# Only run if this flag is set. The flag is created by the yum-cron init
# script when the service is started -- this allows one to use chkconfig and
# the standard "service stop|start" commands to enable or disable yum-cron.
if [[ ! -f /var/lock/subsys/yum-cron ]]; then
  exit 0
fi

# Action!
exec /usr/sbin/yum-cron
#############################################################################



4. Configure the settings for weekly run.
#cd /etc/yum
#ls
#vi yum-cron-weekly.conf
[commands]
update_cmd = default
random_sleep = 4320
update_messages = yes
download_updates = yes
apply_updates = yes
[emitters]
emit_via = email,plugins
emit_when_up_to_date = yes
emitters_dir = /etc/yum-cron.emitters.d <Not found>
[base]
reposdir = /etc/yum-cron.repos.d
[email]
email_from = servername
email_to = teams channel
email_host = smtprr.cerner.com

5. Mention repo in yum-cron.repos.d
#cd /etc/yum-cron.repos.d <Not found>
#ls
Common-x86_64.repo

6. Contents of Common-x86_64.repo
#cat Common-x86_64.repo

# This file was generated by Chef. --- "Something just like this"
# Do NOT modify this file by hand.

[Common-x86_64]
name=Common - x86_64
baseurl=https://cernerrepos.net/yum/oracle-linux/oel/6/current/x86_64/Common/
enabled=1
fastestmirror_enabled=0
gpgcheck=1
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-oracle-ol6


7.
#cat 0yum-weekly.cron
#!/bin/bash

# Only run if this flag is set. The flag is created by the yum-cron init
# script when the service is started -- this allows one to use chkconfig and
# the standard "service stop|start" commands to enable or disable yum-cron.
if [[ ! -f /var/lock/subsys/yum-cron ]]; then
  exit 0
fi

# Action!
exec /usr/sbin/yum-cron /etc/yum/yum-cron-weekly.conf

8) chown root:root /etc/cron.weekly/0yum-weekly.cron
9) chmod 0750 /etc/cron.weekly/0yum-weekly.cron 
10) create or modify /etc/sysconfig/yum-cron to have ONLY the following:

RANDOMWAIT="60"
DAYS_OF_WEEK="0123456"

11.Check for the cronjob
sudo grep yum /var/log/cron
